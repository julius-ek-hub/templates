import { ReactNode } from 'react';
import { DialogProps as MuiDialogProps } from '@mui/material';

/**
 * Customized Dialog base component
 * @type {{title: ReactNode, action: ReactNode} & MuiDialogProps}
 */

export let DialogProps;

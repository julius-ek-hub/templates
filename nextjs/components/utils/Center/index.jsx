import { Box, BoxProps } from '@mui/material';

/**
 * @arg {BoxProps} props
 */

const Center = (props) => <Box display="flex" alignItems="center" justifyContent="center" {...props} />
export default Center;